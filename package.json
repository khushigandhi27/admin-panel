{
  "name": "admin-panel-fullstack",
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix admin-panel-frontend && npm install --prefix admin-panel-backend",
    "build": "npm run build --prefix admin-panel-frontend",
    "start": "NODE_ENV=production node admin-panel-backend/index.js"
  },
  "engines": {
    "node": "18.x"
  }
}
